import { Text, View } from 'react-native';

const Profile = () => {
	return (
		<View style={{ padding: 20 }}>
			<Text style={{ fontSize: 24 }}>Profile View</Text>
		</View>
	);
};

export default Profile;